import React from "react";
import { TextProps } from "./textModel";
declare const Text: ({ type, size, url, target, hover, headingAs, color, truncate, textDecoration, children, textAlign, fontWeight, }: TextProps) => React.JSX.Element | null;
export default Text;
