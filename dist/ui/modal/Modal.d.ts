import React from "react";
import { ModalPropsTypes } from "./modalModel";
declare const Modal: ({ title, children, buttonText, closeButtonText, buttonVariant, size, loading, onClose, onClickFunc, }: ModalPropsTypes) => React.JSX.Element;
export default Modal;
