import React from "react";
import { CardHeaderProps } from "./cardHeaderModel";
declare const CardHeader: ({ children, justifyContent, border, padding, customPadding, className }: CardHeaderProps) => React.JSX.Element;
export default CardHeader;
