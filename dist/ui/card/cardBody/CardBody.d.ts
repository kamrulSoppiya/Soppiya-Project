import React from "react";
import { CardBodyProps } from "./cardBodyModel";
declare const CardBody: ({ children, border, padding, customPadding, className, onClick }: CardBodyProps) => React.JSX.Element;
export default CardBody;
