import React from "react";
import { CardFooterProps } from "./cardFooterModel";
declare const CardFooter: ({ children, justifyContent, border, padding, customPadding, className }: CardFooterProps) => React.JSX.Element;
export default CardFooter;
