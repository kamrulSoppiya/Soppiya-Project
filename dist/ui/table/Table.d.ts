import React from 'react';
import { RadioProps } from './tableModel';
declare const Table: ({ children, scroll, border, borderColor, bgColor, headerBgColor, hover, striped, size }: RadioProps) => React.JSX.Element;
export default Table;
